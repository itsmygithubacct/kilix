# Route only web hyperlinks through Kilix's ordered browser policy. Other URL
# schemes retain Kitty's normal handlers.
protocol http,https
action launch --type=background -- ${KILIX_HOME}/kilix open-url $URL
