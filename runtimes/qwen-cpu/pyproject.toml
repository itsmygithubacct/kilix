[project]
name = "kilix-qwen-tts-cpu-runtime"
version = "0.2.2"
requires-python = ">=3.13,<3.14"
dependencies = [
  "qwen-tts==0.1.1",
  "torch==2.6.0+cpu",
  "torchaudio==2.6.0+cpu",
  "transformers==4.57.3",
  "accelerate==1.12.0",
]

[tool.uv]
package = false
environments = ["sys_platform == 'linux' and platform_machine == 'x86_64'"]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
torchaudio = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true
